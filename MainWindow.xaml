<Window x:Class="FileSearcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" Drop="Window_Drop" AllowDrop="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="tbTarget" FontSize="20"/>
        <TextBox x:Name="tbPath" Grid.Row="1" FontSize="20"/>
        <ListBox x:Name="listResult" Grid.Row="2" MouseDoubleClick="listResult_MouseDoubleClick"/>
        <Button Content="Search" Grid.Row="3" Click="Button_Click_1"/>
    </Grid>
</Window>
